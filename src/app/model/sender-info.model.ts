export interface PersonInfo {
    phone: string;
    city: string;
    index: string;

    address: string;
    contact: string;
}

export interface PackageInfo {
    description: string;
    cost: string;
}